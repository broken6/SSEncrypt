# SSEncrypt 插件更新文档

更新日期：2026-08-11
版本：1.2.3
构建产物：`target/SSEncrypt-1.2.3.jar`

## 一、v1.2.3 主要改动

### 1. Decrypted 标签多格式视图

解密结果支持 4 个视图切换查看：

- **Raw**：解密原文（可编辑，`getMessage()`/`isModified()`/`getSelectedData()` 均以 Raw 为准）
- **Pretty**：语法高亮美化视图（`RSyntaxTextArea`，idea 主题，代码折叠），按正文自动识别 JSON / XML / HTML / SQL / 纯文本
- **Hex**：hexdump（行号 gutter + 十六进制 + ASCII），支持 hex ↔ ASCII **双向选中联动**；UTF-8 中文正确显示
- **Unicode**：解码 `\uXXXX` 为实际字符（含代理对），无 unicode 转义时保持原文

### 2. 修复问题

- **Decrypted 标签无限嵌套**：改用 `ITextEditor`（纯文本编辑器）替代 `createMessageEditor()`，从根上消除 Burp 标签工厂递归嵌套；保留 `contentLock`/`lastDisplayedContent`/`taskEpoch` 防重入
- **Hex 列错位**：多字节 UTF-8 字符跨 16 字节行边界导致行长不一致，改为 hex 列严格 16 字节/行、ASCII 列用字形（Glyph）单独解析
- **Hex 行号被选中**：偏移量拆到独立左侧 gutter 列（不可聚焦），选中只含 hex 内容

## 二、影响范围

| 文件 | 改动 |
| --- | --- |
| `pom.xml` | 版本 1.2.3 |
| `src/main/java/burp/core/BurpSSEPlugin.java` | 扩展名 v1.2.3 |
| `src/main/java/burp/extension/MessageEditorTab.java` | 多格式视图、Pretty 渲染、Unicode 解码 |
| `src/main/java/burp/extension/HexViewerPanel.java` | 新增：Hex 视图（行号 gutter + 双向选中联动） |

## 三、重新加载步骤

1. Burp **Extender → Extensions** 移除旧插件
2. 加载 `target/SSEncrypt-1.2.3.jar`
3. 确认扩展名 `SSEncrypt v1.2.3`

## 四、验证

- Decrypted 标签在 Raw / Pretty / Hex / Unicode 间切换正常
- Hex 视图选中左侧 hex 右侧 ASCII 同步高亮，反之亦然
- 不再出现嵌套标签栏；中文内容 hex 显示对齐
